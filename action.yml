name: 'Create Hotfix 2nd PR'
description: 'Open second PR if branch is hotfix'
author: 'kibibit'
inputs:
  token: # change this
    required: true
    description: 'github token'
runs:
  using: 'node12'
  main: 'dist/index.js'
